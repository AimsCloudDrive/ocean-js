# 变更日志

## 0.0.1

### 新增

- 初始化 model-designer 子项目
- 引入本地子项目：`@msom/xbuild`、`@msom/common`、`@msom/dom`、`@msom/component`、`@msom/reaction`
- 提供 `ModelDesigner` 示例组件与 `mountModelDesigner` 挂载工具
- 提供 `ModelNode`、`ModelDesignerOption` 类型定义
- 配置 `xbuild` 构建与 `tsc` 类型检查
